services:
  - type: web
    name: camsentinel-vms
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: ENC_KEY
        generateValue: true
      - key: PORT
        value: 10000
      - key: FFMPEG_PATH
        value: /usr/bin/ffmpeg
      - key: ANPR_ENABLED
        value: true
    disk:
      name: recordings
      mountPath: /app/storage
      sizeGB: 10